{
  "name": "@repo/definitions",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "build": "tsx src/index.ts && pnpm run check-types && tsup --clean",
    "lint": "eslint ."
  },
  "dependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "dayjs": "^1.11.13",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "tsx": "^4.19.4"
  }
}
